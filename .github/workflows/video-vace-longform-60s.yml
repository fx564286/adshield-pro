name: video-vace-longform-60s

on:
  push:
    branches:
      - video-toolchain-probe
    paths:
      - .github/workflows/video-vace-longform-60s.yml
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - name: Validate workflow skeleton
        run: |
          set -euxo pipefail
          echo VACE_60S_WORKFLOW_SCHEMA=PASS
