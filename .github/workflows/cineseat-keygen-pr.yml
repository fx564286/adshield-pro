name: Cineseat Legacy Secure Builder Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy encrypted builder disabled; alpha20 V5 is authoritative.'
